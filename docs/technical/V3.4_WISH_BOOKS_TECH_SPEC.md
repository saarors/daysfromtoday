# V3.4 Wish Books（愿望宝典）技术方案文档

**版本**：V3.4.0  
**创建日期**：2025-01-07  
**状态**：待审核 → 开发中 → 已完成  
**技术负责人**：Leon  

---

## 📋 目录

1. [技术架构](#一技术架构)
2. [数据库设计](#二数据库设计)
3. [API 设计](#三api-设计)
4. [前端实现](#四前端实现)
5. [富文本编辑器](#五富文本编辑器)
6. [图片视频处理](#六图片视频处理)
7. [SEO 实现](#七seo-实现)
8. [性能优化](#八性能优化)
9. [部署方案](#九部署方案)
10. [开发计划](#十开发计划)

---

## 一、技术架构

### 1.1 整体架构图

```
┌─────────────────────────────────────────────────────────┐
│                    用户访问层                            │
│              /zh/wish-books/[slug]                      │
└─────────────────────┬───────────────────────────────────┘
                      │
                      ↓
┌─────────────────────────────────────────────────────────┐
│                  Next.js 15 (App Router)                │
│  ┌──────────────────┐  ┌──────────────────┐            │
│  │  聚合页 (SSG)    │  │  详情页 (SSG+ISR)│            │
│  │  /wish-books     │  │  /wish-books/    │            │
│  │                  │  │  [slug]          │            │
│  └──────────────────┘  └──────────────────┘            │
└─────────────────────┬───────────────────────────────────┘
                      │
                      ↓
┌─────────────────────────────────────────────────────────┐
│                    管理后台层                            │
│              /admin/wish-books                          │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐                │
│  │ 卡片列表 │ │ 创建编辑 │ │ 预览发布 │                │
│  └──────────┘ └──────────┘ └──────────┘                │
└─────────────────────┬───────────────────────────────────┘
                      │
                      ↓
┌─────────────────────────────────────────────────────────┐
│                    API 层                               │
│  /api/wish-books (CRUD)                                 │
│  /api/upload-image (图片上传)                            │
│  /api/upload-video (视频上传)                            │
│  /api/revalidate (强制刷新)                              │
└─────────────────────┬───────────────────────────────────┘
                      │
        ┌─────────────┴─────────────┐
        ↓                           ↓
┌──────────────────┐     ┌──────────────────┐
│  Supabase        │     │  Cloudflare R2   │
│  (数据库)        │     │  (图片/视频存储)  │
│  - wish_books    │     │  - 封面图         │
│  - wish_tags     │     │  - 配图           │
│                  │     │  - 视频           │
└──────────────────┘     └──────────────────┘
```

---

### 1.2 技术栈

| 层级 | 技术 | 说明 |
|------|------|------|
| **前端框架** | Next.js 15 | App Router, TypeScript, React 18 |
| **样式** | Tailwind CSS v4 | 现有设计系统 |
| **富文本编辑器** | Tiptap v2 | 开源 WYSIWYG 编辑器 |
| **表单管理** | React Hook Form | 轻量、高性能 |
| **数据库** | Supabase PostgreSQL | 现有基础设施 |
| **文件存储** | Cloudflare R2 | 图片/视频存储 |
| **认证** | Supabase Auth | 现有用户系统 |
| **部署** | Vercel Edge Functions | 现有部署平台 |

---

### 1.3 核心特性

| 特性 | 实现方式 |
|------|---------|
| **静态生成** | generateStaticParams + SSG |
| **增量静态再生** | revalidate = 3600 (1小时) |
| **动态回退** | dynamicParams = true |
| **图片优化** | Next.js Image + WebP |
| **视频优化** | Cloudflare R2 + 自适应播放器 |
| **SEO 优化** | generateMetadata + JSON-LD |
| **性能优化** | 懒加载 + CDN + Edge Caching |

---

## 二、数据库设计

### 2.1 主表：`wish_books`

```sql
CREATE TABLE wish_books (
  -- 基础信息
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  
  -- 标题与 URL
  title TEXT NOT NULL,                        -- 「100天跑马拉松」
  subtitle TEXT,                              -- 副标题
  slug VARCHAR(200) UNIQUE NOT NULL,          -- URL slug: '100-days-marathon'
  
  -- 核心维度
  time_dimension VARCHAR(20) NOT NULL,        -- '7-days', '30-days', '90-days', '100-days'...
  duration_days INTEGER NOT NULL,             -- 实际天数
  category VARCHAR(50) NOT NULL,              -- 'fitness', 'learning', 'career'...
  subcategory VARCHAR(100),                   -- 'marathon', 'guitar'...
  
  -- 目标元数据
  goal_type VARCHAR(30),                      -- 'challenge', 'habit', 'project', 'milestone'
  difficulty VARCHAR(20),                     -- 'easy', 'medium', 'hard', 'expert'
  target_audience TEXT[],                     -- ['运动新人', '自我突破型']
  
  -- 内容（富文本）
  story_intro TEXT,                           -- 故事引入（HTML 或 Markdown）
  content_html TEXT,                          -- 主要内容（HTML）
  content_json JSONB,                         -- Tiptap JSON 格式（原始数据）
  
  -- 结构化内容
  roadmap JSONB,                              -- 路线图
  /*
    {
      "phases": [
        {
          "phase": 1,
          "name": "基础训练",
          "duration": "第1-4周",
          "goals": ["..."],
          "milestones": ["..."]
        }
      ]
    }
  */
  
  real_cases JSONB,                           -- 真实案例
  ai_suggestions JSONB,                       -- AI 建议（V3.2 助手）
  resources JSONB,                            -- 资源清单
  faqs JSONB,                                 -- FAQ
  related_goals TEXT[],                       -- 相关目标（slug 数组）
  
  -- 图片与媒体
  cover_image_url TEXT,                       -- 封面图
  hero_image_url TEXT,                        -- 详情页头图
  og_image_url TEXT,                          -- OG 分享图
  media_urls JSONB,                           -- 其他图片/视频 URL
  
  -- SEO 元数据
  meta_title TEXT,
  meta_description TEXT,
  keywords TEXT[],
  
  -- 统计数据
  view_count INTEGER DEFAULT 0,
  save_count INTEGER DEFAULT 0,
  share_count INTEGER DEFAULT 0,
  
  -- 状态管理
  status VARCHAR(20) DEFAULT 'draft',         -- 'draft', 'published', 'archived'
  content_source VARCHAR(20) DEFAULT 'manual', -- 'manual', 'ai_generated', 'ugc'
  quality_score FLOAT DEFAULT 0,              -- 0-100
  
  -- 多语言
  locale VARCHAR(10) DEFAULT 'zh',
  
  -- 时间戳
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW(),
  published_at TIMESTAMP,
  
  -- 作者
  author_id UUID REFERENCES auth.users(id),
  
  -- 索引
  CONSTRAINT valid_status CHECK (status IN ('draft', 'published', 'archived')),
  CONSTRAINT valid_difficulty CHECK (difficulty IN ('easy', 'medium', 'hard', 'expert'))
);

-- 索引优化
CREATE INDEX idx_wish_books_slug ON wish_books(slug);
CREATE INDEX idx_wish_books_status ON wish_books(status);
CREATE INDEX idx_wish_books_time_dimension ON wish_books(time_dimension);
CREATE INDEX idx_wish_books_category ON wish_books(category);
CREATE INDEX idx_wish_books_published ON wish_books(published_at DESC) WHERE status = 'published';
CREATE INDEX idx_wish_books_quality ON wish_books(quality_score DESC) WHERE status = 'published';

-- 全文搜索索引
CREATE INDEX idx_wish_books_search ON wish_books USING GIN(to_tsvector('english', title || ' ' || COALESCE(subtitle, '')));
```

---

### 2.2 辅助表：`wish_tags`

```sql
CREATE TABLE wish_tags (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  wish_book_id UUID REFERENCES wish_books(id) ON DELETE CASCADE,
  tag VARCHAR(50) NOT NULL,
  created_at TIMESTAMP DEFAULT NOW(),
  
  UNIQUE(wish_book_id, tag)
);

CREATE INDEX idx_wish_tags_book_id ON wish_tags(wish_book_id);
CREATE INDEX idx_wish_tags_tag ON wish_tags(tag);
```

---

### 2.3 数据库迁移脚本

**文件**：`supabase/migrations/20250107_create_wish_books.sql`

```sql
-- Enable UUID extension
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- Create wish_books table
CREATE TABLE wish_books (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  title TEXT NOT NULL,
  subtitle TEXT,
  slug VARCHAR(200) UNIQUE NOT NULL,
  time_dimension VARCHAR(20) NOT NULL,
  duration_days INTEGER NOT NULL,
  category VARCHAR(50) NOT NULL,
  subcategory VARCHAR(100),
  goal_type VARCHAR(30),
  difficulty VARCHAR(20),
  target_audience TEXT[],
  story_intro TEXT,
  content_html TEXT,
  content_json JSONB,
  roadmap JSONB,
  real_cases JSONB,
  ai_suggestions JSONB,
  resources JSONB,
  faqs JSONB,
  related_goals TEXT[],
  cover_image_url TEXT,
  hero_image_url TEXT,
  og_image_url TEXT,
  media_urls JSONB,
  meta_title TEXT,
  meta_description TEXT,
  keywords TEXT[],
  view_count INTEGER DEFAULT 0,
  save_count INTEGER DEFAULT 0,
  share_count INTEGER DEFAULT 0,
  status VARCHAR(20) DEFAULT 'draft',
  content_source VARCHAR(20) DEFAULT 'manual',
  quality_score FLOAT DEFAULT 0,
  locale VARCHAR(10) DEFAULT 'zh',
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW(),
  published_at TIMESTAMP,
  author_id UUID,
  
  CONSTRAINT valid_status CHECK (status IN ('draft', 'published', 'archived')),
  CONSTRAINT valid_difficulty CHECK (difficulty IN ('easy', 'medium', 'hard', 'expert'))
);

-- Create indexes
CREATE INDEX idx_wish_books_slug ON wish_books(slug);
CREATE INDEX idx_wish_books_status ON wish_books(status);
CREATE INDEX idx_wish_books_time_dimension ON wish_books(time_dimension);
CREATE INDEX idx_wish_books_category ON wish_books(category);
CREATE INDEX idx_wish_books_published ON wish_books(published_at DESC) WHERE status = 'published';
CREATE INDEX idx_wish_books_quality ON wish_books(quality_score DESC) WHERE status = 'published';
CREATE INDEX idx_wish_books_search ON wish_books USING GIN(to_tsvector('english', title || ' ' || COALESCE(subtitle, '')));

-- Create wish_tags table
CREATE TABLE wish_tags (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  wish_book_id UUID REFERENCES wish_books(id) ON DELETE CASCADE,
  tag VARCHAR(50) NOT NULL,
  created_at TIMESTAMP DEFAULT NOW(),
  UNIQUE(wish_book_id, tag)
);

CREATE INDEX idx_wish_tags_book_id ON wish_tags(wish_book_id);
CREATE INDEX idx_wish_tags_tag ON wish_tags(tag);

-- Enable Row Level Security (RLS)
ALTER TABLE wish_books ENABLE ROW LEVEL SECURITY;
ALTER TABLE wish_tags ENABLE ROW LEVEL SECURITY;

-- RLS Policies for wish_books
-- 公开读取已发布的内容
CREATE POLICY "Public can read published wish books"
  ON wish_books FOR SELECT
  USING (status = 'published');

-- 管理员可以读取所有内容
CREATE POLICY "Admins can read all wish books"
  ON wish_books FOR SELECT
  USING (auth.jwt() ->> 'role' = 'admin');

-- 管理员可以创建
CREATE POLICY "Admins can insert wish books"
  ON wish_books FOR INSERT
  WITH CHECK (auth.jwt() ->> 'role' = 'admin');

-- 管理员可以更新
CREATE POLICY "Admins can update wish books"
  ON wish_books FOR UPDATE
  USING (auth.jwt() ->> 'role' = 'admin');

-- 管理员可以删除
CREATE POLICY "Admins can delete wish books"
  ON wish_books FOR DELETE
  USING (auth.jwt() ->> 'role' = 'admin');

-- RLS Policies for wish_tags
CREATE POLICY "Public can read tags"
  ON wish_tags FOR SELECT
  USING (true);

CREATE POLICY "Admins can manage tags"
  ON wish_tags FOR ALL
  USING (auth.jwt() ->> 'role' = 'admin');

-- Create function to update updated_at timestamp
CREATE OR REPLACE FUNCTION update_updated_at_column()
RETURNS TRIGGER AS $$
BEGIN
  NEW.updated_at = NOW();
  RETURN NEW;
END;
$$ LANGUAGE plpgsql;

-- Create trigger
CREATE TRIGGER update_wish_books_updated_at
  BEFORE UPDATE ON wish_books
  FOR EACH ROW
  EXECUTE FUNCTION update_updated_at_column();

-- Insert sample time dimensions (for reference)
COMMENT ON COLUMN wish_books.time_dimension IS '时间维度：7-days, 30-days, 90-days, 100-days, 180-days, 365-days, 3-years, 5-years';
COMMENT ON COLUMN wish_books.category IS '类别：fitness, learning, career, personal_growth, relationships, creative, lifestyle, mental_health';
```

---

## 三、API 设计

### 3.1 RESTful API 端点

| 方法 | 端点 | 功能 | 权限 |
|------|------|------|------|
| GET | `/api/wish-books` | 获取卡片列表（支持筛选） | Public |
| GET | `/api/wish-books/[id]` | 获取单个卡片详情 | Public |
| POST | `/api/wish-books` | 创建新卡片 | Admin |
| PATCH | `/api/wish-books/[id]` | 更新卡片 | Admin |
| DELETE | `/api/wish-books/[id]` | 删除卡片（软删除）| Admin |
| POST | `/api/wish-books/[id]/publish` | 发布卡片 | Admin |
| POST | `/api/upload-image` | 上传图片到 R2 | Admin |
| POST | `/api/upload-video` | 上传视频到 R2 | Admin |
| POST | `/api/revalidate` | 强制刷新静态页面 | Admin |

---

### 3.2 API 实现

#### 3.2.1 GET `/api/wish-books`

**功能**：获取卡片列表（支持筛选、分页、搜索）

**查询参数**：
```typescript
interface GetWishBooksQuery {
  status?: 'draft' | 'published' | 'archived';
  time_dimension?: string;
  category?: string;
  difficulty?: string;
  search?: string;
  page?: number;
  limit?: number;
  sort?: 'latest' | 'popular' | 'quality';
}
```

**响应**：
```typescript
interface GetWishBooksResponse {
  data: WishBook[];
  pagination: {
    page: number;
    limit: number;
    total: number;
    totalPages: number;
  };
}
```

**实现**：
```typescript
// app/api/wish-books/route.ts

import { createClient } from '@/lib/supabase/server';
import { NextRequest, NextResponse } from 'next/server';

export async function GET(request: NextRequest) {
  const { searchParams } = new URL(request.url);
  
  // 解析查询参数
  const status = searchParams.get('status') || 'published';
  const timeDimension = searchParams.get('time_dimension');
  const category = searchParams.get('category');
  const difficulty = searchParams.get('difficulty');
  const search = searchParams.get('search');
  const page = parseInt(searchParams.get('page') || '1');
  const limit = parseInt(searchParams.get('limit') || '12');
  const sort = searchParams.get('sort') || 'latest';
  
  const supabase = createClient();
  
  // 构建查询
  let query = supabase
    .from('wish_books')
    .select('*', { count: 'exact' })
    .eq('status', status);
  
  // 筛选条件
  if (timeDimension) {
    query = query.eq('time_dimension', timeDimension);
  }
  
  if (category) {
    query = query.eq('category', category);
  }
  
  if (difficulty) {
    query = query.eq('difficulty', difficulty);
  }
  
  // 搜索
  if (search) {
    query = query.or(`title.ilike.%${search}%,subtitle.ilike.%${search}%`);
  }
  
  // 排序
  if (sort === 'latest') {
    query = query.order('published_at', { ascending: false });
  } else if (sort === 'popular') {
    query = query.order('view_count', { ascending: false });
  } else if (sort === 'quality') {
    query = query.order('quality_score', { ascending: false });
  }
  
  // 分页
  const from = (page - 1) * limit;
  const to = from + limit - 1;
  query = query.range(from, to);
  
  // 执行查询
  const { data, error, count } = await query;
  
  if (error) {
    return NextResponse.json({ error: error.message }, { status: 500 });
  }
  
  return NextResponse.json({
    data,
    pagination: {
      page,
      limit,
      total: count || 0,
      totalPages: Math.ceil((count || 0) / limit)
    }
  });
}
```

---

#### 3.2.2 POST `/api/wish-books`

**功能**：创建新卡片

**请求体**：
```typescript
interface CreateWishBookRequest {
  title: string;
  subtitle?: string;
  slug: string;
  time_dimension: string;
  duration_days: number;
  category: string;
  subcategory?: string;
  goal_type?: string;
  difficulty?: string;
  target_audience?: string[];
  content_html?: string;
  content_json?: any;
  roadmap?: any;
  real_cases?: any;
  ai_suggestions?: any;
  resources?: any;
  faqs?: any;
  related_goals?: string[];
  cover_image_url?: string;
  meta_title?: string;
  meta_description?: string;
  keywords?: string[];
}
```

**实现**：
```typescript
// app/api/wish-books/route.ts

export async function POST(request: NextRequest) {
  const supabase = createClient();
  
  // 验证管理员权限
  const { data: { user }, error: authError } = await supabase.auth.getUser();
  if (authError || !user) {
    return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });
  }
  
  // 解析请求体
  const body = await request.json();
  
  // 验证必填字段
  if (!body.title || !body.slug || !body.time_dimension || !body.category) {
    return NextResponse.json(
      { error: 'Missing required fields' },
      { status: 400 }
    );
  }
  
  // 插入数据库
  const { data, error } = await supabase
    .from('wish_books')
    .insert({
      ...body,
      author_id: user.id,
      status: 'draft',
      created_at: new Date().toISOString()
    })
    .select()
    .single();
  
  if (error) {
    return NextResponse.json({ error: error.message }, { status: 500 });
  }
  
  return NextResponse.json(data, { status: 201 });
}
```

---

#### 3.2.3 PATCH `/api/wish-books/[id]`

**功能**：更新卡片

**实现**：
```typescript
// app/api/wish-books/[id]/route.ts

export async function PATCH(
  request: NextRequest,
  { params }: { params: { id: string } }
) {
  const supabase = createClient();
  
  // 验证权限
  const { data: { user }, error: authError } = await supabase.auth.getUser();
  if (authError || !user) {
    return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });
  }
  
  const body = await request.json();
  
  // 更新数据库
  const { data, error } = await supabase
    .from('wish_books')
    .update({
      ...body,
      updated_at: new Date().toISOString()
    })
    .eq('id', params.id)
    .select()
    .single();
  
  if (error) {
    return NextResponse.json({ error: error.message }, { status: 500 });
  }
  
  // 如果是已发布的卡片，触发重新验证
  if (data.status === 'published') {
    await revalidatePath(`/wish-books/${data.slug}`);
  }
  
  return NextResponse.json(data);
}
```

---

#### 3.2.4 POST `/api/wish-books/[id]/publish`

**功能**：发布卡片（状态从 draft → published）

**实现**：
```typescript
// app/api/wish-books/[id]/publish/route.ts

import { revalidatePath, revalidateTag } from 'next/cache';

export async function POST(
  request: NextRequest,
  { params }: { params: { id: string } }
) {
  const supabase = createClient();
  
  // 验证权限
  const { data: { user }, error: authError } = await supabase.auth.getUser();
  if (authError || !user) {
    return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });
  }
  
  // 更新状态为 published
  const { data, error } = await supabase
    .from('wish_books')
    .update({
      status: 'published',
      published_at: new Date().toISOString(),
      updated_at: new Date().toISOString()
    })
    .eq('id', params.id)
    .select()
    .single();
  
  if (error) {
    return NextResponse.json({ error: error.message }, { status: 500 });
  }
  
  // 触发静态页面生成/更新
  await revalidatePath(`/wish-books/${data.slug}`);
  await revalidatePath('/wish-books'); // 更新聚合页
  await revalidateTag('wish-books'); // 更新所有相关缓存
  
  return NextResponse.json({
    success: true,
    data,
    message: `卡片已发布：${data.slug}`
  });
}
```

---

### 3.3 图片上传 API

#### POST `/api/upload-image`

**功能**：上传图片到 Cloudflare R2

**实现**：
```typescript
// app/api/upload-image/route.ts

import { S3Client, PutObjectCommand } from '@aws-sdk/client-s3';
import { NextRequest, NextResponse } from 'next/server';
import crypto from 'crypto';

const s3Client = new S3Client({
  region: 'auto',
  endpoint: `https://${process.env.CLOUDFLARE_ACCOUNT_ID}.r2.cloudflarestorage.com`,
  credentials: {
    accessKeyId: process.env.CLOUDFLARE_R2_ACCESS_KEY_ID!,
    secretAccessKey: process.env.CLOUDFLARE_R2_SECRET_ACCESS_KEY!
  }
});

export async function POST(request: NextRequest) {
  try {
    const formData = await request.formData();
    const file = formData.get('file') as File;
    
    if (!file) {
      return NextResponse.json({ error: 'No file provided' }, { status: 400 });
    }
    
    // 验证文件类型
    const allowedTypes = ['image/jpeg', 'image/png', 'image/webp', 'image/gif'];
    if (!allowedTypes.includes(file.type)) {
      return NextResponse.json(
        { error: 'Invalid file type. Only JPEG, PNG, WebP, and GIF are allowed.' },
        { status: 400 }
      );
    }
    
    // 验证文件大小（最大 5MB）
    if (file.size > 5 * 1024 * 1024) {
      return NextResponse.json(
        { error: 'File too large. Maximum size is 5MB.' },
        { status: 400 }
      );
    }
    
    // 生成唯一文件名
    const fileExtension = file.name.split('.').pop();
    const fileName = `wish-books/${crypto.randomUUID()}.${fileExtension}`;
    
    // 转换为 Buffer
    const arrayBuffer = await file.arrayBuffer();
    const buffer = Buffer.from(arrayBuffer);
    
    // 上传到 R2
    await s3Client.send(new PutObjectCommand({
      Bucket: process.env.CLOUDFLARE_R2_BUCKET_NAME,
      Key: fileName,
      Body: buffer,
      ContentType: file.type
    }));
    
    // 返回公开 URL
    const publicUrl = `${process.env.NEXT_PUBLIC_R2_PUBLIC_URL}/${fileName}`;
    
    return NextResponse.json({
      success: true,
      url: publicUrl,
      fileName
    });
    
  } catch (error: any) {
    console.error('Upload error:', error);
    return NextResponse.json(
      { error: 'Upload failed', details: error.message },
      { status: 500 }
    );
  }
}
```

---

## 四、前端实现

### 4.1 聚合页

#### 文件结构

```
app/[locale]/wish-books/
├── page.tsx              # 聚合页（Server Component）
├── components/
│   ├── WishBookCard.tsx  # 卡片组件
│   ├── FilterBar.tsx     # 筛选栏
│   └── SearchBar.tsx     # 搜索框
└── loading.tsx           # 加载状态
```

#### 实现

```typescript
// app/[locale]/wish-books/page.tsx

import { Metadata } from 'next';
import { getWishBooks } from '@/lib/wish-books';
import WishBookCard from './components/WishBookCard';
import FilterBar from './components/FilterBar';
import SearchBar from './components/SearchBar';

export async function generateMetadata({ params }): Promise<Metadata> {
  const { locale } = await params;
  
  return {
    title: '愿望宝典 - 用时间记录人类目标与实现之路 | DaysFromToday',
    description: '探索 7 天到 5 年的人生目标百科，从健身、学习到职业成长，看看别人是怎么做到的。',
    keywords: ['目标计划', '时间管理', '愿望清单', '人生规划', '目标百科'],
    alternates: {
      canonical: `/${locale}/wish-books`,
      languages: {
        'zh': '/zh/wish-books',
        'en': '/en/wish-books'
      }
    },
    openGraph: {
      title: '愿望宝典 | DaysFromToday',
      description: '用时间记录人类目标与实现之路',
      type: 'website',
      images: ['/og-wish-books.jpg']
    }
  };
}

export default async function WishBooksPage({ searchParams }) {
  const { locale } = await params;
  const filters = await searchParams;
  
  // 从数据库获取卡片列表
  const wishBooks = await getWishBooks({
    status: 'published',
    time_dimension: filters.time_dimension,
    category: filters.category,
    difficulty: filters.difficulty,
    search: filters.search,
    locale
  });
  
  return (
    <div className="container mx-auto py-12">
      {/* Hero Section */}
      <section className="text-center mb-12">
        <h1 className="text-5xl font-bold mb-4 bg-gradient-to-r from-pink-500 via-purple-500 to-blue-500 bg-clip-text text-transparent">
          愿望宝典
        </h1>
        <p className="text-xl text-gray-400 max-w-3xl mx-auto mb-8">
          用时间记录人类目标与实现之路<br />
          从 7 天到 5 年，探索时间的可能性
        </p>
        
        {/* 搜索框 */}
        <SearchBar />
      </section>
      
      {/* 筛选栏 */}
      <FilterBar />
      
      {/* 卡片网格 */}
      <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 mt-8">
        {wishBooks.map(book => (
          <WishBookCard key={book.id} book={book} />
        ))}
      </div>
      
      {/* 空状态 */}
      {wishBooks.length === 0 && (
        <div className="text-center py-20">
          <p className="text-xl text-gray-400">
            暂无符合条件的卡片
          </p>
        </div>
      )}
    </div>
  );
}

// 静态生成
export const revalidate = 3600; // 1小时重新验证
```

---

### 4.2 详情页

```typescript
// app/[locale]/wish-books/[slug]/page.tsx

import { Metadata } from 'next';
import { notFound } from 'next/navigation';
import { getWishBook, getAllWishBookSlugs } from '@/lib/wish-books';
import Image from 'next/image';
import Link from 'next/link';
import RoadmapTimeline from './components/RoadmapTimeline';
import RealCasesGallery from './components/RealCasesGallery';
import ResourcesGrid from './components/ResourcesGrid';
import FAQAccordion from './components/FAQAccordion';

export async function generateStaticParams() {
  const slugs = await getAllWishBookSlugs();
  
  return slugs.flatMap(slug => [
    { slug, locale: 'zh' },
    { slug, locale: 'en' }
  ]);
}

export const dynamicParams = true; // 允许动态生成新页面
export const revalidate = 3600; // 1小时重新验证

export async function generateMetadata({ params }): Promise<Metadata> {
  const { slug, locale } = await params;
  const book = await getWishBook(slug, locale);
  
  if (!book) return { title: 'Not Found' };
  
  return {
    title: book.meta_title || `${book.title} | 愿望宝典`,
    description: book.meta_description || book.subtitle,
    keywords: book.keywords,
    alternates: {
      canonical: `/${locale}/wish-books/${slug}`,
      languages: {
        'zh': `/zh/wish-books/${slug}`,
        'en': `/en/wish-books/${slug}`
      }
    },
    openGraph: {
      title: book.title,
      description: book.subtitle,
      type: 'article',
      images: [book.og_image_url || book.cover_image_url],
      publishedTime: book.published_at,
      modifiedTime: book.updated_at
    }
  };
}

export default async function WishBookDetailPage({ params }) {
  const { slug, locale } = await params;
  const book = await getWishBook(slug, locale);
  
  if (!book) return notFound();
  
  // 增加浏览次数
  await incrementViewCount(book.id);
  
  return (
    <article className="container mx-auto py-12 max-w-4xl">
      {/* Breadcrumb */}
      <nav className="mb-6 text-sm text-gray-500">
        <Link href="/wish-books">愿望宝典</Link>
        <span className="mx-2">/</span>
        <span>{book.title}</span>
      </nav>
      
      {/* Hero Section */}
      <header className="mb-8">
        <div className="flex gap-2 mb-4">
          <span className="px-3 py-1 bg-blue-500/20 rounded-full text-sm">
            ⏰ {TIME_DIMENSIONS[book.time_dimension].label}
          </span>
          <span className="px-3 py-1 bg-purple-500/20 rounded-full text-sm">
            {CATEGORIES[book.category].icon} {CATEGORIES[book.category].name}
          </span>
          <span className="px-3 py-1 bg-green-500/20 rounded-full text-sm">
            💪 {DIFFICULTY_LABELS[book.difficulty]}
          </span>
        </div>
        
        <h1 className="text-5xl font-bold mb-4">{book.title}</h1>
        <p className="text-2xl text-gray-400 mb-6">{book.subtitle}</p>
        
        <div className="flex gap-6 text-gray-500 mb-6">
          <span>👁️ {formatNumber(book.view_count)} 阅读</span>
          <span>❤️ {formatNumber(book.save_count)} 收藏</span>
          <span>📤 {formatNumber(book.share_count)} 分享</span>
        </div>
        
        {/* CTA 按钮 */}
        <div className="flex gap-4">
          <Link
            href={`/wishlist?days=${book.duration_days}&goalText=${book.title}`}
            className="btn btn-primary btn-lg"
          >
            🚀 创建我的计划
          </Link>
          <button className="btn btn-secondary">
            ❤️ 收藏
          </button>
          <button className="btn btn-ghost">
            📤 分享
          </button>
        </div>
      </header>
      
      {/* Hero Image */}
      {book.hero_image_url && (
        <div className="relative h-[400px] rounded-2xl overflow-hidden mb-12">
          <Image
            src={book.hero_image_url}
            alt={book.title}
            fill
            className="object-cover"
            priority
          />
        </div>
      )}
      
      {/* 故事引入 */}
      <section className="prose prose-lg max-w-none mb-12">
        <div dangerouslySetInnerHTML={{ __html: book.story_intro }} />
      </section>
      
      {/* 路线图 */}
      {book.roadmap && (
        <section className="mb-12">
          <h2 className="text-3xl font-bold mb-6">🧭 路线图</h2>
          <RoadmapTimeline roadmap={book.roadmap} />
        </section>
      )}
      
      {/* 真实案例 */}
      {book.real_cases && book.real_cases.length > 0 && (
        <section className="mb-12">
          <h2 className="text-3xl font-bold mb-6">🧠 真实案例</h2>
          <RealCasesGallery cases={book.real_cases} />
        </section>
      )}
      
      {/* AI 建议 */}
      {book.ai_suggestions && (
        <section className="mb-12">
          <h2 className="text-3xl font-bold mb-6">💬 AI 助手建议</h2>
          <div className="bg-gradient-to-r from-purple-500/10 to-blue-500/10 rounded-2xl p-8">
            <p className="text-lg italic mb-4">"{book.ai_suggestions.quote}"</p>
            <ul className="space-y-2">
              {book.ai_suggestions.key_principles?.map((principle, i) => (
                <li key={i}>✓ {principle}</li>
              ))}
            </ul>
          </div>
        </section>
      )}
      
      {/* 资源清单 */}
      {book.resources && (
        <section className="mb-12">
          <h2 className="text-3xl font-bold mb-6">🔗 资源清单</h2>
          <ResourcesGrid resources={book.resources} />
        </section>
      )}
      
      {/* FAQ */}
      {book.faqs && book.faqs.length > 0 && (
        <section className="mb-12">
          <h2 className="text-3xl font-bold mb-6">❓ 常见问题</h2>
          <FAQAccordion faqs={book.faqs} />
        </section>
      )}
      
      {/* 延伸阅读 */}
      {book.related_goals && book.related_goals.length > 0 && (
        <section className="mb-12">
          <h2 className="text-3xl font-bold mb-6">🔗 延伸阅读</h2>
          <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
            {book.related_goals.map(relatedSlug => (
              <RelatedCard key={relatedSlug} slug={relatedSlug} />
            ))}
          </div>
        </section>
      )}
      
      {/* JSON-LD */}
      <script
        type="application/ld+json"
        dangerouslySetInnerHTML={{
          __html: JSON.stringify({
            '@context': 'https://schema.org',
            '@type': 'Article',
            headline: book.title,
            description: book.subtitle,
            image: book.og_image_url || book.hero_image_url,
            datePublished: book.published_at,
            dateModified: book.updated_at,
            author: {
              '@type': 'Organization',
              name: 'DaysFromToday'
            }
          })
        }}
      />
      
      {/* FAQ Schema */}
      {book.faqs && book.faqs.length > 0 && (
        <script
          type="application/ld+json"
          dangerouslySetInnerHTML={{
            __html: JSON.stringify({
              '@context': 'https://schema.org',
              '@type': 'FAQPage',
              mainEntity: book.faqs.map(faq => ({
                '@type': 'Question',
                name: faq.question,
                acceptedAnswer: {
                  '@type': 'Answer',
                  text: faq.answer
                }
              }))
            })
          }}
        />
      )}
    </article>
  );
}
```

---

## 五、富文本编辑器

### 5.1 技术选型：Tiptap v2

**为什么选择 Tiptap？**
- ✅ 开源免费（MIT License）
- ✅ 基于 ProseMirror（强大的底层引擎）
- ✅ 完全可定制
- ✅ 支持 Markdown
- ✅ 支持图片/视频拖拽上传
- ✅ TypeScript 支持
- ✅ 活跃的社区

---

### 5.2 安装依赖

```bash
npm install @tiptap/react @tiptap/starter-kit @tiptap/extension-image @tiptap/extension-link @tiptap/extension-youtube
```

---

### 5.3 编辑器组件

```typescript
// components/admin/TiptapEditor.tsx

'use client';

import { useEditor, EditorContent } from '@tiptap/react';
import StarterKit from '@tiptap/starter-kit';
import Image from '@tiptap/extension-image';
import Link from '@tiptap/extension-link';
import Youtube from '@tiptap/extension-youtube';
import { useState } from 'react';

interface TiptapEditorProps {
  content: string;
  onChange: (html: string, json: any) => void;
}

export default function TiptapEditor({ content, onChange }: TiptapEditorProps) {
  const [isUploading, setIsUploading] = useState(false);
  
  const editor = useEditor({
    extensions: [
      StarterKit,
      Image,
      Link.configure({
        openOnClick: false
      }),
      Youtube.configure({
        width: 640,
        height: 360
      })
    ],
    content,
    onUpdate: ({ editor }) => {
      const html = editor.getHTML();
      const json = editor.getJSON();
      onChange(html, json);
    }
  });
  
  if (!editor) {
    return <div>Loading editor...</div>;
  }
  
  // 图片上传处理
  const handleImageUpload = async (file: File) => {
    setIsUploading(true);
    
    try {
      const formData = new FormData();
      formData.append('file', file);
      
      const response = await fetch('/api/upload-image', {
        method: 'POST',
        body: formData
      });
      
      const data = await response.json();
      
      if (data.success) {
        editor.chain().focus().setImage({ src: data.url }).run();
      } else {
        alert('上传失败：' + data.error);
      }
    } catch (error) {
      alert('上传失败');
    } finally {
      setIsUploading(false);
    }
  };
  
  return (
    <div className="border rounded-lg overflow-hidden">
      {/* 工具栏 */}
      <div className="bg-gray-100 p-2 flex gap-2 flex-wrap">
        <button
          onClick={() => editor.chain().focus().toggleBold().run()}
          className={`btn btn-sm ${editor.isActive('bold') ? 'btn-active' : ''}`}
        >
          B
        </button>
        <button
          onClick={() => editor.chain().focus().toggleItalic().run()}
          className={`btn btn-sm ${editor.isActive('italic') ? 'btn-active' : ''}`}
        >
          I
        </button>
        <button
          onClick={() => editor.chain().focus().toggleHeading({ level: 2 }).run()}
          className={`btn btn-sm ${editor.isActive('heading', { level: 2 }) ? 'btn-active' : ''}`}
        >
          H2
        </button>
        <button
          onClick={() => editor.chain().focus().toggleBulletList().run()}
          className={`btn btn-sm ${editor.isActive('bulletList') ? 'btn-active' : ''}`}
        >
          • List
        </button>
        <button
          onClick={() => {
            const url = prompt('Image URL:');
            if (url) {
              editor.chain().focus().setImage({ src: url }).run();
            }
          }}
          className="btn btn-sm"
        >
          🖼️ Image
        </button>
        <label className="btn btn-sm">
          📤 Upload
          <input
            type="file"
            accept="image/*"
            className="hidden"
            onChange={(e) => {
              const file = e.target.files?.[0];
              if (file) handleImageUpload(file);
            }}
          />
        </label>
        <button
          onClick={() => {
            const url = prompt('YouTube URL:');
            if (url) {
              editor.commands.setYoutubeVideo({ src: url });
            }
          }}
          className="btn btn-sm"
        >
          ▶️ YouTube
        </button>
      </div>
      
      {/* 编辑区域 */}
      <EditorContent
        editor={editor}
        className="prose max-w-none p-4 min-h-[400px]"
      />
      
      {/* 上传提示 */}
      {isUploading && (
        <div className="absolute inset-0 bg-black/50 flex items-center justify-center">
          <div className="bg-white p-4 rounded-lg">
            <p>上传中...</p>
          </div>
        </div>
      )}
    </div>
  );
}
```

---

## 六、图片视频处理

### 6.1 图片优化流程

```
用户上传
    ↓
前端验证（类型、大小）
    ↓
上传到 Cloudflare R2
    ↓
返回公开 URL
    ↓
Next.js Image 组件优化
    ↓
用户访问（WebP、懒加载、响应式）
```

---

### 6.2 Next.js Image 配置

```typescript
// next.config.js

module.exports = {
  images: {
    domains: [
      'cdn.daysfromtoday.ai',
      process.env.NEXT_PUBLIC_R2_PUBLIC_URL?.replace('https://', '')
    ],
    formats: ['image/webp', 'image/avif'],
    deviceSizes: [640, 750, 828, 1080, 1200, 1920, 2048, 3840],
    imageSizes: [16, 32, 48, 64, 96, 128, 256, 384]
  }
};
```

---

### 6.3 视频处理

**选项 1：直接存储在 R2**
- 优点：简单、成本低
- 缺点：无转码、无自适应流

**选项 2：Cloudflare Stream**
- 优点：自动转码、HLS/DASH、质量自适应
- 缺点：额外成本（$5/1000 分钟）

**推荐**：MVP 阶段使用 R2，未来迁移到 Stream。

---

## 七、SEO 实现

### 7.1 Sitemap 生成

```typescript
// app/(sitemaps)/sitemap-wish-books.xml/route.ts

import { createClient } from '@/lib/supabase/server';

export async function GET() {
  const supabase = createClient();
  
  // 获取所有已发布的卡片
  const { data: wishBooks } = await supabase
    .from('wish_books')
    .select('slug, updated_at, locale')
    .eq('status', 'published')
    .order('updated_at', { ascending: false });
  
  const sitemap = `<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <!-- 聚合页 -->
  <url>
    <loc>https://www.daysfromtoday.ai/zh/wish-books</loc>
    <changefreq>weekly</changefreq>
    <priority>0.9</priority>
  </url>
  <url>
    <loc>https://www.daysfromtoday.ai/en/wish-books</loc>
    <changefreq>weekly</changefreq>
    <priority>0.9</priority>
  </url>
  
  <!-- 详情页 -->
  ${wishBooks?.map(book => `
  <url>
    <loc>https://www.daysfromtoday.ai/${book.locale}/wish-books/${book.slug}</loc>
    <lastmod>${new Date(book.updated_at).toISOString()}</lastmod>
    <changefreq>monthly</changefreq>
    <priority>0.8</priority>
  </url>`).join('')}
</urlset>`;
  
  return new Response(sitemap, {
    headers: {
      'Content-Type': 'application/xml',
      'Cache-Control': 'public, max-age=3600, s-maxage=3600'
    }
  });
}
```

---

## 八、性能优化

### 8.1 性能目标

| 指标 | 目标 |
|------|------|
| LCP（最大内容绘制）| < 2.0s |
| INP（交互延迟）| < 200ms |
| CLS（布局偏移）| < 0.1 |
| TTFB（首字节时间）| < 500ms |

---

### 8.2 优化措施

| 优化项 | 实现方式 |
|--------|---------|
| **静态生成** | SSG + ISR，减少服务器渲染 |
| **图片优化** | Next.js Image + WebP + 懒加载 |
| **代码分割** | 动态导入（dynamic import）|
| **CDN 缓存** | Vercel Edge Network |
| **数据库查询** | 索引优化 + 缓存 |
| **字体优化** | next/font 自动优化 |

---

## 九、部署方案

### 9.1 环境变量

```bash
# .env.production

# Site
NEXT_PUBLIC_SITE_URL=https://www.daysfromtoday.ai

# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Cloudflare R2
CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_R2_ACCESS_KEY_ID=
CLOUDFLARE_R2_SECRET_ACCESS_KEY=
CLOUDFLARE_R2_BUCKET_NAME=daysfromtoday-media
NEXT_PUBLIC_R2_PUBLIC_URL=https://cdn.daysfromtoday.ai
```

---

### 9.2 部署流程

```bash
# 1. 本地测试
npm run dev:safe

# 2. 构建测试
npm run build
npm run start

# 3. 提交代码
git add .
git commit -m "feat: V3.4 Wish Books 完整实现"
git push origin main

# 4. Vercel 自动部署
# （Vercel 检测到 push，自动构建和部署）

# 5. 验证部署
curl https://www.daysfromtoday.ai/zh/wish-books
```

---

## 十、开发计划

### 10.1 时间表（17天 / 3周）

| 阶段 | 天数 | 任务 | 交付物 |
|------|------|------|--------|
| **Phase 1** | Day 1-3 | 数据库 + API | 表结构 + CRUD API |
| **Phase 2** | Day 4-8 | 管理后台 | 列表、编辑、预览 |
| **Phase 3** | Day 9-12 | 前台页面 | 聚合页 + 详情页 |
| **Phase 4** | Day 13-14 | SEO 优化 | Sitemap + JSON-LD |
| **Phase 5** | Day 15-17 | 测试部署 | 上线发布 |

---

### 10.2 详细任务清单

#### Phase 1: 数据库 + API（Day 1-3）

**Day 1**：
- [ ] 创建数据库迁移脚本
- [ ] 运行迁移，创建 `wish_books` 和 `wish_tags` 表
- [ ] 配置 RLS 策略
- [ ] 测试数据库连接

**Day 2**：
- [ ] 实现 GET `/api/wish-books` (列表)
- [ ] 实现 GET `/api/wish-books/[id]` (详情)
- [ ] 实现 POST `/api/wish-books` (创建)
- [ ] 测试 API 端点

**Day 3**：
- [ ] 实现 PATCH `/api/wish-books/[id]` (更新)
- [ ] 实现 DELETE `/api/wish-books/[id]` (删除)
- [ ] 实现 POST `/api/wish-books/[id]/publish` (发布)
- [ ] 实现 POST `/api/upload-image` (图片上传)
- [ ] 完整 API 测试

---

#### Phase 2: 管理后台（Day 4-8）

**Day 4**：
- [ ] 创建后台布局和导航
- [ ] 实现卡片列表页（表格展示）
- [ ] 实现筛选和搜索功能

**Day 5**：
- [ ] 创建"新建卡片"表单
- [ ] 实现基础信息字段（标题、类别、时间维度等）
- [ ] 实现表单验证

**Day 6-7**：
- [ ] 集成 Tiptap 富文本编辑器
- [ ] 实现图片拖拽上传
- [ ] 实现视频 URL 嵌入
- [ ] 实现工具栏功能

**Day 8**：
- [ ] 实现预览功能
- [ ] 实现保存草稿
- [ ] 实现发布功能
- [ ] 后台完整测试

---

#### Phase 3: 前台页面（Day 9-12）

**Day 9-10**：
- [ ] 实现聚合页布局
- [ ] 实现卡片网格组件
- [ ] 实现筛选栏
- [ ] 实现搜索功能
- [ ] 实现分页/无限滚动

**Day 11-12**：
- [ ] 实现详情页布局
- [ ] 实现各个内容区块组件
- [ ] 实现响应式设计
- [ ] 实现动画和交互
- [ ] 前台完整测试

---

#### Phase 4: SEO 优化（Day 13-14）

**Day 13**：
- [ ] 实现 generateMetadata（动态元数据）
- [ ] 实现 JSON-LD 结构化数据
- [ ] 实现 Open Graph 和 Twitter Cards
- [ ] 实现 Canonical 和 hreflang

**Day 14**：
- [ ] 实现 Sitemap 生成
- [ ] 配置 robots.txt
- [ ] 实现内链推荐
- [ ] SEO 验证和测试

---

#### Phase 5: 测试和部署（Day 15-17）

**Day 15**：
- [ ] 功能完整测试
- [ ] 不同设备测试（桌面、平板、移动）
- [ ] 不同浏览器测试
- [ ] Bug 修复

**Day 16**：
- [ ] 性能测试（Lighthouse）
- [ ] 加载速度优化
- [ ] SEO 验证（Google Search Console）
- [ ] 准备生产环境

**Day 17**：
- [ ] 部署到 Vercel
- [ ] 验证生产环境
- [ ] 创建首批 10 个示范卡片
- [ ] 提交 Sitemap 到 Google
- [ ] 版本发布和文档更新

---

## 附录

### A. 数据库 Schema 完整版

见 `supabase/migrations/20250107_create_wish_books.sql`

### B. API 端点完整文档

见 `docs/api/WISH_BOOKS_API.md`（待创建）

### C. 组件库文档

见 `docs/components/WISH_BOOKS_COMPONENTS.md`（待创建）

---

## 版本历史

| 版本 | 日期 | 变更说明 | 作者 |
|------|------|---------|------|
| 1.0 | 2025-01-07 | 初始版本，完整技术方案 | Claude |

---

**下一步**：等待 Leon 审核 PRD 和技术方案，确认后开始开发。








