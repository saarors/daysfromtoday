---
alwaysApply: true
---

[项目总则]
本规则仅适用于 daysfromtoday 项目。目标：让 AI Coding 理解本项目的产品定位、技术架构、编码规范、沟通方式与安全边界。

---------------------------------------
🧭 一、产品定位
- 产品名称：DaysFromToday  
- 项目类型：多语言日期计算网站  
- 产品目标：用户打开页面即可获得“X天后（前）是哪天”的答案。  
- 核心功能：自然日计算、工作日计算、DST与节假日感知、ICS文件下载、多语言(i18n)、SEO规模化、AdSense变现。  
- 用户体验：开页即答案；可一键下载或加到日历；自动识别国家与时区。

---------------------------------------
⚙️ 二、技术栈与架构
- 前端框架：Next.js 15 (App Router, TypeScript, Edge Runtime)  
- 样式系统：Tailwind CSS + next/font  
- 时间计算：date-fns + date-fns-tz（IANA TZ，DST安全）  
- 国际化：next-intl（多语言路由+本地化文本）  
- 节假日API：Nager.Date（缓存24h）  
- 部署平台：Vercel（CI/CD + Edge Functions）  
- 配置中心：Vercel Edge Config  
- 域名管理：Cloudflare（灰云 DNS Only）  
- 数据层：MVP阶段无数据库。  
- SEO组件：Metadata API、hreflang、sitemap、robots、FAQ JSON-LD、OG图。  
- 性能红线(CWV)：LCP≤2.5s，INP≤200ms，CLS≤0.1。

---------------------------------------
🧱 三、代码规范与协作
1. 编写语言：TypeScript（严格模式）。  
2. 风格工具：Prettier + ESLint；禁止 any 滥用。  
3. 文件命名：小写中划线，如 `answer-card.tsx`。  
4. 函数命名：驼峰格式，如 `getHolidays()`。  
5. 每次改动输出三项内容：  
   - 变更清单（修改/新增的文件与原因）  
   - 风险分析（SEO、性能、安全影响）  
   - 测试与验收步骤（Definition of Done）  
6. 新增依赖前须说明用途、体积、替代方案与安全性。  
7. 提交信息遵循 Conventional Commits：`feat`、`fix`、`chore`、`docs`。

---------------------------------------
💬 四、沟通与输出方式
1. 所有回答统一使用中文。  
2. 解释应“小学六年级能懂”，但保留专业术语（如 i18n、ISR、RFC5545、CWV 等）。  
3. 输出必须结构化，格式为：目标 → 原理 → 步骤 → 验收标准。  
4. 若问题存在歧义，先列“待确认点 (To Confirm)”再执行。  
5. 每次输出可引用 docs/ai 文件夹的文档内容。  

---------------------------------------
🧠 五、AI 模型分工
- Claude 4.5 Sonnet：主脑，负责逻辑分析、架构设计、重构与代码审查。  
- GPT-5 (High/Medium)：执行层，生成 Next.js + TypeScript 代码。  
- DeepSeek v3.1：辅助层，批量页面/文案生成与低成本处理。

---------------------------------------
🔐 六、安全与合规
1. 不修改 DNS 或代理相关配置（Cloudflare 灰云模式保持）。  
2. 广告：仅使用 Google AdSense；根目录需有 `/ads.txt`；EU/UK/CH 流量必须启用 CMP + Consent Mode v2。  
3. 默认不收集用户数据或 IP；仅用于即时计算。  
4. CSP 变更须先使用 Report-Only 模式，确认后再收紧。  
5. 禁止添加追踪/埋点脚本，除非明确说明合法来源。  
6. 禁止输出任何包含 API Key、密钥或私人路径的信息。  

---------------------------------------
📚 七、知识来源与上下文
- 所有设计与代码规范以项目内 `docs/ai/*.md` 文档为准；  
- 若存在冲突，优先遵守本 Project Rule；  
- 本规则可被 AI 在 Cursor 中自动读取，用于所有 Chat 与 Coding 操作。

---------------------------------------
🏁 八、任务执行标准
AI 每次执行开发任务时必须：
1. 先生成执行计划（Plan），说明要改的文件与意图；  
2. 产出代码前展示 diff 预览；  
3. 输出测试与验证方法；  
4. 确认 Definition of Done 达成。  

---------------------------------------
✅ 九、最终目标
- 代码干净、规范、可读；
- 网站快速、安全、SEO友好；
- 满足商业变现（AdSense）与多语言扩展需求；
- 让任何 AI 都能在此规则下接手开发而不跑偏。